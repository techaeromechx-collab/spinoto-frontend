# Rust build artifacts — several GB. Must never enter the repo.
/target/

# Generated by tauri-build at compile time (schemas, permission manifests).
# Regenerated on every build; committing it causes noisy diffs and conflicts.
/gen/
